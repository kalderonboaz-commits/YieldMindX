# Documentation placeholders only; map names to your selected runtime.
# Browser-exposed configuration is public. Never expose DATABASE_URL to the frontend.
PUBLIC_API_BASE_URL=
# Server-only; populate locally from your chosen database connection settings.
DATABASE_URL=
