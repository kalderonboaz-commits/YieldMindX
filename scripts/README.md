# Repository automation

Add repeatable setup, build and verification scripts once tooling is selected. Document required tools and inputs. Keep secrets outside scripts and fail clearly when prerequisites are absent. The scaffold includes no executable scripts.
