# Backend

Backend workspace; language/runtime intentionally unselected. src/modules holds business capabilities and local unit tests; src/platform holds runtime and external adapters. tests/ holds API/database integration. Backend owns validation, authorization and private database access. Add executable manifests only after stack selection.
