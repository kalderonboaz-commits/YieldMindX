# Frontend

Frontend workspace (React is an example). Choose framework/build tool before adding a manifest. Keep feature code and unit/component tests together; reserve tests/ for frontend integration. public/ holds public assets only. No server secrets belong here. See [structure](../../project-managment/07_software_structure.md).
